interface:
  display_name: "ICP Backwards Mapping and Assessment"
  short_description: "Adapt ICP tasks into defensible evidence"
  brand_color: "#0057B8"
  default_prompt: "Adapt a mainstream task into a defensible ICP evidence package."
policy:
  allow_implicit_invocation: false
